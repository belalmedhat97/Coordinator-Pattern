//
//  CoordinatorInit.swift
//  coordinatorPattern
//
//  Created by belal medhat on 16/01/2023.
//

import UIKit
//MARK: - for example an enum for the logged status so if user logged succesfully save in user default a key of value (AuthStatus.logged) if not save value (AuthStatus.notLogged)
enum AuthStatus{
    case logged
    case notLogged
}







